# Copy of /usr/src/mediatek-mt7927-2.12/dkms.conf with BUILD_EXCLUSIVE_KERNEL="^7\.0\." added 2026-08-28
# so dkms.service skips (not fails) on kernels the 2.12 source cannot build for. Reapply after reinstalling the rpm.
